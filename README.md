to compile the program, run: make cvcswn

place the files you want to save in the '/input/' directory
rebuilt files will appear in the '/output/' directory

commands to use the program:

./cvcswn ingest [names of the files]
    ^saves the specified files

./cvcswn rebuild [names of the files]
    ^rebuilds the saved files with the specified names
